(ns kata2-chop-binary-search.core)

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(defn chop [value array]
  -1)

; (defn chop [x coll]
;   (let [result (keep-indexed 
;                 (fn [idx item]
;                   (if (= item x) idx))
;                 coll)]
;     (if result
;       (first result))))